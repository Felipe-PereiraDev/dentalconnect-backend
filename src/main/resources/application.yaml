spring:
  application:
    name: dentalconnect
  profiles:
    active: dev
server:
  error:
    include-stacktrace: never
    include-message: NEVER
    include-binding-errors: never
key:
  geocoding-api: ${KEY_GEOCODING_API}
