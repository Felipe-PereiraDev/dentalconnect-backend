services:
  database:
    image: postgres:15-alpine
    container_name: postgresql
    environment:
      POSTGRES_DB: dentalconnect
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"